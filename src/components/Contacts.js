import React from "react";

import { StyledSection } from "../lib/Styling";

const Contacts = () => {
  return (
    <StyledSection>This is the contact page</StyledSection>
  )
};

export default Contacts;